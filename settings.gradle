pluginManagement {
    repositories {
        gradlePluginPortal()
        google()
        mavenCentral()
    }
}

rootProject.name = "MyApplication"
include ':app'

// 为所有项目配置仓库
gradle.allprojects {
    repositories {
        google()
        mavenCentral()
    }
} 
